vbox = {
	name = "variable_list_inspector_plugin"
	layoutpolicy_horizontal = preferred
	layoutpolicy_vertical = expanding

	datacontext = "[VariableListInspectorPlugin.AccessListStore]"

	variable_list_store = {
		layoutpolicy_horizontal = preferred
		layoutpolicy_vertical = expanding	
	}
}