select_menu_left = {
	blockoverride "menu_setup" {
		name = "select_heir_selection"
	}

	blockoverride "close_on_action" { on_action = "[SelectHeirSelection.OnClose]" }
	blockoverride "select_content" {
		expand = {}
	}
}
